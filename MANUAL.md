# MovieEdit 操作マニュアル

ブラウザ上で動画をタイムラインに並べ、HeyGen HyperFrames で MP4 に書き出す動画編集アプリの操作マニュアルです。大容量アップロードと、一言指示による自律ハイライト編集に対応しています。

- 概要と設計は [README.md](./README.md) を参照
- このマニュアルは **機能一覧** と **操作方法** をまとめたものです

---

## 0. どこで動かすの？（大前提）

- **この Claude の画面はコードを「作る」場所**です。ここでは動きません。
- MovieEdit は **あなたのPC（Windows など）で動かすプログラム**です。操作は **Windows の「コマンドプロンプト」または「PowerShell」**（＝いわゆる DOS 画面）に文字コマンドを打って行います。
- 全体像：**① PCに準備（1回だけ） → ② コマンドで起動 → ③ ブラウザで使う**

---

## 0.5 はじめて動かす（Windows・初心者向け）

### 準備するもの（1回だけ）
1. **Node.js（v22以上）をインストール** — [https://nodejs.org/](https://nodejs.org/) の「LTS」をダウンロードして、案内どおり「次へ」で進めるだけ。
2. **コードをPCに置く** — 次のどちらか。
   - 簡単な方法（Gitなし）：GitHub のリポジトリ画面で緑の **「Code」▼ → 「Download ZIP」** → 保存 → **展開（すべて展開）**。例：`C:\Users\あなた\Downloads\MovieEdit`
   - Git がある方：`git clone <リポジトリURL>`

### 起動する（毎回）
1. **PowerShell を開く**：スタートメニューで「PowerShell」と検索して起動。
   （または展開したフォルダを開き、上部アドレスバーに `powershell` と入力して Enter でもOK）
2. **アプリのフォルダに移動**（`cd` の後ろは、あなたの実際のフォルダの場所）：
   ```powershell
   cd C:\Users\あなた\Downloads\MovieEdit
   ```
   ※ フォルダをエクスプローラで開き、**アドレスバーのパスをコピー**して貼り付けると確実です。
3. **必要部品を取得**（初回のみ・数分かかります。Chromium と ffmpeg も自動で入ります）：
   ```powershell
   npm install
   ```
4. **起動**：
   ```powershell
   npm start
   ```
   `MovieEdit → http://localhost:4000` と表示されれば成功。**このウィンドウは開いたまま**にします（閉じると停止）。
5. **ブラウザ**（Chrome/Edge）で **`http://localhost:4000`** を開く → 編集画面が出ます。

### 止め方・次回
- 止めるとき：PowerShell のウィンドウで **Ctrl + C**、またはウィンドウを閉じる。
- 次回からは **手順 1→2→4→5**（`npm install` は初回だけでOK）。

> うまくいかないときは、PowerShell に出た**赤い文字（エラー）をそのままコピーして教えてください**。原因を特定して直します。

---

## 1'. 起動（要点まとめ）

```powershell
npm install     # 初回だけ（Chromium・ffmpeg も自動取得）
npm start       # → http://localhost:4000
```
ブラウザで `http://localhost:4000` を開きます。
（Mac / Linux の場合はターミナルで同じコマンド。既存の Chromium を使う等の詳細は README を参照）

### 画面構成（3カラム）
```
┌─ ヘッダー ────────────────────────────────────────────────┐
│ 🎬 MovieEdit   [プロジェクト選択 ▼] [+ New project]        │
├───────────────┬──────────────────────┬────────────────────┤
│ Media library │ Timeline             │ 🤖 Auto-edit jobs  │
│ （素材・      │ （編集エリア）       │ Renders            │
│   アップロード）│                     │ （書き出し）       │
└───────────────┴──────────────────────┴────────────────────┘
```

| エリア | 役割 |
| --- | --- |
| **Media library**（左） | 動画のアップロード、素材一覧、AIステータス表示 |
| **Timeline**（中央） | クリップ／テロップの追加・編集、プレビュー、レンダリング |
| **Auto-edit / Renders**（右） | 自律編集ジョブと書き出しの進捗・ダウンロード |

---

## 1. 機能一覧

| # | 機能 | 説明 | 場所 |
| --- | --- | --- | --- |
| 1 | **プロジェクト管理** | 解像度・fps を持つ編集単位を作成／切替 | ヘッダー |
| 2 | **チャンク・アップロード** | 大容量動画を分割送信。回線断・リロードでも自動再開 | Media library |
| 3 | **素材ライブラリ** | アップロード済み動画の一覧（尺・解像度・音声有無を表示） | Media library |
| 4 | **クリップ追加** | 素材をタイムラインへ配置 | Media library → 「+ Timeline」 |
| 5 | **テキスト（テロップ）追加** | 任意の文字テロップを追加 | Timeline → 「+ Text」 |
| 6 | **要素編集** | 開始位置・長さ・トラック・文字・サイズ・色を編集、削除 | Timeline |
| 7 | **プレビュー** | ブラウザ内でざっくり再生確認（別タブ） | Timeline → 「▶ Preview」 |
| 8 | **レンダリング** | HyperFrames で決定論的に MP4 書き出し＋ダウンロード | Timeline → 「⬇ Render MP4」 |
| 9 | **🤖 自律ハイライト編集** | 一言の指示だけで、ハイライト抽出＋テロップ／装飾付けを自動実行 | Media library → 「🤖 Auto-edit」 |
| 10 | **AIステータス表示** | ハイライト立案／音声認識の有効・無効を表示 | Media library 上部 |

### 素材の種類（タイムライン要素）
| 種類 | 内容 |
| --- | --- |
| video | 動画クリップ（音声を同期再生） |
| text | テロップ。プリセット: `title`（大見出し）/ `caption`（下部字幕）/ `lower-third`（名前バー）/ `pill`（丸タグ） |
| decoration | 装飾。プリセット: `accent-bar` / `vignette` / `progress` / `corner-label` |
| audio / image | 音声・画像（API経由で追加可能） |

---

## 2. 操作方法（基本フロー）

### 手順①：プロジェクトを作る
1. ヘッダーの **「+ New project」** をクリック
2. プロジェクト名を入力（既定は 1920×1080 / 30fps）
3. 以降、ヘッダーの **プロジェクト選択 ▼** で切り替えられます

### 手順②：動画をアップロードする（大容量ファイルの入れ方）

**方法A：ブラウザにドラッグ＆ドロップ（基本）**
1. 左パネルの点線エリアに動画を **ドラッグ＆ドロップ**、または **「Choose file」** で選択
2. 進捗バーが表示され、分割送信されます（大容量でもメモリを圧迫しません）
   - 途中で失敗しても自動リトライ。ページを再読み込みしても続きから再開します
3. 完了すると素材一覧に追加され、尺・解像度・音声有無が表示されます

**方法B：既にディスク上にある巨大ファイルを直接取り込む（アップロード不要・最速）**
- MovieEdit を動かしているマシン上にファイルがある場合（例：5時間の録画）は、CLIでその場参照（シンボリックリンク）として登録できます。コピーしないので追加ディスクも待ち時間もほぼゼロです。
```bash
node scripts/import-asset.js /path/to/long-recording.mp4
# 字幕も同様に取り込めます
node scripts/import-asset.js /path/to/subtitles.srt
```
- 実行後にブラウザを更新すると Media library に表示されます。

> どちらの方法でも、まずは **1〜2分の短いクリップ**で「取り込み→編集→書き出し」を一周してから本番の長尺を扱うのがおすすめです。

### 手順③：クリップを並べる
1. 素材一覧の項目にある **「+ Timeline」** をクリック → タイムラインに動画クリップが追加されます
2. 複数クリップを追加すると順に並びます

### 手順④：テロップ（文字）を入れる
1. 中央パネルの **「+ Text」** をクリック
2. テロップ文言を入力 → タイムラインにテキスト要素が追加されます

### 手順⑤：タイミングや見た目を調整する
各要素カードの入力欄で編集します（変更は即保存）。

| 項目 | 意味 |
| --- | --- |
| **start (s)** | 表示開始（秒） |
| **duration (s)** | 表示の長さ（秒。空欄なら素材の長さ） |
| **track** | 重なり順（数値が大きいほど前面。同トラックの動画は重なりません） |
| **text / font px / color** | テロップの文字・サイズ・色（テキスト要素のみ） |

不要な要素は各カード右上の **「✕ remove」** で削除します。

### 手順⑥：プレビューする
- **「▶ Preview」** をクリックすると別タブでブラウザ内プレビューが開きます
- クリップの表示切替・動画再生・テロップのフェードをおおまかに確認できます（最終品質はレンダリング結果が正）

### 手順⑦：書き出す（レンダリング）
1. **「⬇ Render MP4」** をクリック → 右パネルの **Renders** にジョブが表示されます
2. 進捗が 0→100% で進みます
3. 完了すると **「⬇ download MP4」** リンクが出るのでクリックしてダウンロード

---

## 3. 操作方法（🤖 自律ハイライト編集）

「長い動画を渡して一言指示するだけ」で、ハイライト抽出＋テロップ・装飾付けまで自動実行します。

### 使い方
1. 左パネルの素材一覧で、対象動画の **「🤖 Auto-edit」** をクリック
2. 順に入力します:
   - **編集指示**（例：`5分のハイライトにして。テロップや装飾も文脈にあわせてつけて。`）
   - **目標の長さ（分）**（例：`5`）
   - **字幕アセットID（任意）**：SRT/VTT をアップロード済みならその素材ID。無ければ空でOK
3. 右パネルの **「🤖 Auto-edit jobs」** に進捗が表示されます

### 内部で自動実行される流れ
```
文字起こし取得 → ハイライト選定＋テロップ/装飾立案 → 区間切り出し
        → プロジェクト自動生成 → レンダリング
```
- 進捗行に **planner**（立案の方式）と **transcript**（文字起こしの入手元）が表示されます
- 完了すると **「open project ▸」** が出ます。クリックすると自動生成されたプロジェクトが編集画面に開くので、**人手で微調整** してから再レンダリングも可能です
- 生成物の書き出しは通常どおり **Renders** に表示され、完了後にダウンロードできます

### 字幕を使ってテロップ精度を上げたい場合
文脈に合ったテロップには文字起こしが必要です。音声認識キー未設定でも、**SRT/VTT 字幕を先にアップロード**（動画と同じ手順でアップロード＝素材化）し、その素材IDを Auto-edit の「字幕アセットID」に指定すれば、字幕テキストからテロップが作られます。

### AIの有効化（任意・高精度化）
左パネル上部の「AI: …」表示で現在の状態を確認できます。

| 役割 | 設定する環境変数 | 未設定時の挙動 |
| --- | --- | --- |
| ハイライト選定・テロップ執筆 | `ANTHROPIC_API_KEY`（Claude） | 顕著性スコアリングの簡易ロジックで代替 |
| 音声認識（字幕自動生成） | `OPENAI_API_KEY`（＋任意 `OPENAI_BASE_URL`） | 字幕アップロード or 文字起こし無し（等間隔選定） |

> キーが無くてもパイプライン全体は動作します（テロップ文言や選定精度が上がるだけ）。

---

## 4. よくある質問・トラブルシュート

| 症状 | 対処 |
| --- | --- |
| レンダリングが失敗する | ffmpeg / Chromium が見つかっているか確認。`GET /healthz` で解決済みバイナリを確認できます。Chromium は `PUPPETEER_EXECUTABLE_PATH` で指定 |
| `npm install` がバイナリDLで失敗 | `ffmpeg-static` は任意依存のためインストール自体は成功します。システムの ffmpeg（PATH上）にフォールバックします |
| アップロードが途中で止まった | 自動で再試行・再開します。ページ再読み込み後も続きから再開されます |
| プレビューでテロップが動かない | プレビューは簡易再生です。最終的な見た目はレンダリング結果を確認してください |
| Auto-edit のテロップが単純 | `ANTHROPIC_API_KEY`（Claude）を設定すると文脈に沿った文言になります。字幕/音声認識があると精度が上がります |
| 生成物が大きくディスクを圧迫 | `assets/` `output/` `compositions/` は生成物置き場です（gitignore 済み）。不要ファイルは削除可能 |

### 主な環境変数（`.env.example` 参照）
| 変数 | 既定 | 意味 |
| --- | --- | --- |
| `PORT` | 4000 | HTTPポート |
| `MAX_UPLOAD_BYTES` | 20GB | アップロード上限 |
| `UPLOAD_CHUNK_BYTES` | 8MB | チャンクサイズ |
| `PUPPETEER_EXECUTABLE_PATH` | puppeteer既定 | レンダリング用Chromium |
| `HYPERFRAMES_FFMPEG_PATH` / `HYPERFRAMES_FFPROBE_PATH` | 同梱→PATH | ffmpeg/ffprobe |
| `ANTHROPIC_API_KEY` | 未設定 | Claudeハイライト立案の有効化 |
| `OPENAI_API_KEY` | 未設定 | 音声認識の有効化 |

---

## 5. API早見表（自動化・連携用）

```
# アップロード（チャンク／レジューム）
POST /api/uploads                 { filename, size, mimeType } -> { uploadId, chunkBytes }
PUT  /api/uploads/:id?offset=N     <生バイナリのチャンク>       -> { received }
POST /api/uploads/:id/complete     -> { asset }

# 素材・プロジェクト・要素
GET  /api/assets
GET/POST/PATCH/DELETE  /api/projects[/:id]
POST/PATCH/DELETE      /api/projects/:id/elements[/:elId]

# プレビュー・レンダリング
GET  /api/projects/:id/preview     -> 302 /compositions/:id/index.html?preview=1
POST /api/projects/:id/render      { quality, format } -> { render }
GET  /api/renders/:id              -> { render: { status, progress, outputFile, ... } }
GET  /output/:file                 レンダリング済みMP4のダウンロード

# 自律ハイライト編集
GET  /api/autoedit/capabilities    -> { planner, asr }
POST /api/autoedit                 { assetId, instruction, targetSeconds, subtitleAssetId? } -> { job }
GET  /api/autoedit/:id             -> { job: { status, stage, progress, projectId, renderId, ... } }
```
